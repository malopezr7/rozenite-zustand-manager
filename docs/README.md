# Documentation assets

Drop project screenshots and visual docs here.

The main README references `docs/zustand-manager-plugin.png` via an absolute GitHub raw URL, so save the panel screenshot at exactly that path and it will render on both GitHub and npmjs.com.

```
docs/
└── zustand-manager-plugin.png   # Hero shot of the panel with real data
```
